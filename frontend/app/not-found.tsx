import { NotFoundState } from "@/components/shared/NotFoundState";

export const metadata = {
  title: "Not Found | StackSpend",
};

export default function NotFound() {
  return <NotFoundState />;
}
